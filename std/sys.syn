#lang: es
// std/sys.syn — Interfaz de Sistema para control de procesos usando FFI

importar_c "<stdlib.h>"

// Declaración externa para salida del proceso
externo funcion exit(codigo: int);

// Wrapper seguro en Synapse
func salir(codigo: int) -> nulo {
    exit(codigo);
    retornar;
}
